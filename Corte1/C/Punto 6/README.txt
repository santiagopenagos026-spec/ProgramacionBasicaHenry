#incluir <stdio.h>

int principal() {
    int a, b, c, máx, mín;

    printf("Ingrese tres valores enteros: ");
    scanf("%d %d %d", &a, &b, &c);

    máximo = a;
    si (b > máx) máx = b;
    si (c > máx) máx = c;

    mín = a;
    si (b < min) min = b;
    si (c < min) min = c;

    printf("El valor máximo es: %d\n", max);
    printf("El valor minimo es: %d\n", min);

    devuelve 0;
}
